#!/usr/bin/env bash
# SessionStart hook for the sdcorejs-agent plugin.
# The Node helper emits the bootstrap only. Project context is assembled later
# by the selected skill and never writes during session start.

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec node "${SCRIPT_DIR}/session-start.mjs"
