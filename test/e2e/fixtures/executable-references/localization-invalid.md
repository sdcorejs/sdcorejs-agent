<!-- executable-reference-default: copy-ready -->

This prose leaks <localized text> outside a localization string.

```typescript
// Replace <localized text> later.
export const label = 'Ready';
```
