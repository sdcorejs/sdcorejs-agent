import { EmployeeDetailComponent } from './detail.component';

describe('EmployeeDetailComponent', () => {
  it('should create', () => {
    expect(new EmployeeDetailComponent()).toBeTruthy();
  });
});
