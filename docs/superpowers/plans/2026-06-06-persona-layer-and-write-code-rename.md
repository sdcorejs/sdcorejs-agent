# Persona Layer + write-code Rename — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a cross-cutting persona layer (ask-once tech/non-tech, stored per target project, changes wording + forces infra defaults) and clean up the now-meaningless `07-` prefix on each track's single `write-code` skill.

**Architecture:** Persona is a layer, not a track. One dispatchable skill (`sdcorejs-persona`) asks once and writes `.sdcorejs/persona.md` in the *target* project; a single shared reference (`_refs/shared/persona.md`) holds the behavior contract every skill consults; the three entry files (CLAUDE.md / AGENTS.md / copilot-instructions.md) gain one rule wiring it in. The rename is mechanical: `git mv` three files + fix path references. Mirror dir names derive from `name:` frontmatter (not filename), so the rename leaves both mirrors byte-identical after re-sync.

**Tech Stack:** Markdown skills with YAML frontmatter; `bash .claude/sync-skills.sh` mirror generator; lefthook pre-commit hooks (`sync-skills`, `check-mirror`, `skill-frontmatter`); manual smoke tests in `TESTING.md`.

**Scope boundary (NOT in this plan):** the infra group (`sdcorejs-dockerize` / `sdcorejs-auth` / `sdcorejs-run-guide` + `_refs/infra/` templates) is Plan 2; nestjs write-code packs are Plan 3; `sdcorejs-solution-builder` is Plan 4. This plan ships independently: persona wording + rename, both verifiable by lefthook + grep with no target-project dependency.

**Repo conventions (read once before starting):**
- Skill source of truth: `skills/<area>/*.md`. Mirror is generated — NEVER hand-edit `.claude/skills/` or `plugin/skills/`.
- Every skill `.md` needs frontmatter `name:` (kebab-case) + `description:` (≥10 chars, includes `Triggers -` and `Applies to … Bilingual` tail) + `allowed-tools:`.
- `_refs/**` files have NO frontmatter (copied wholesale, excluded from the frontmatter check).
- After any change under `skills/**` or `_refs/**`: run `bash .claude/sync-skills.sh` then `bash .claude/sync-skills.sh --check`.
- Persona file is written to the **target project's** `.sdcorejs/persona.md`, NOT this agent repo (same rule as auto-docs).

---

## File Structure

| File | Action | Responsibility |
|---|---|---|
| `_refs/shared/persona.md` | Create | Behavior contract: how `tech` vs `non-tech` changes wording + defaults. Loaded on demand by any skill. |
| `skills/orchestration/persona.md` | Create | Dispatchable skill `sdcorejs-persona`: ask-once, write/read `.sdcorejs/persona.md`, idempotent. |
| `CLAUDE.md` | Modify | Add persona to session-start ritual (rule 4), add a new mandatory rule, add skill to the orchestration table. |
| `AGENTS.md` | Modify | Mirror the CLAUDE.md persona rule + skill entry. |
| `.github/copilot-instructions.md` | Modify | Mirror the CLAUDE.md persona rule + skill entry. |
| `TESTING.md` | Modify | Add a persona-dispatch smoke test. |
| `skills/tracks/angular/07-write-code.md` | Rename → `write-code.md` | Drop numeric prefix (single write-code skill per track). |
| `skills/tracks/nestjs/07-write-code.md` | Rename → `write-code.md` | Same. |
| `skills/tracks/nextjs/07-write-code.md` | Rename → `write-code.md` | Same. |
| `CLAUDE.md`, `AGENTS.md`, `.github/copilot-instructions.md`, `README.md`, `TESTING.md` | Modify | Replace path references `07-write-code` → `write-code`. |
| `.claude/skills/**`, `plugin/skills/**`, `.claude/_refs/**`, `plugin/_refs/**` | Generated | Regenerated by `sync-skills.sh`; staged, never hand-edited. |

---

## Task 1: Persona behavior-contract reference

**Files:**
- Create: `_refs/shared/persona.md`

- [ ] **Step 1: Confirm the target path is a no-frontmatter ref dir**

Run: `ls _refs/shared/`
Expected: lists existing refs (`review-accessibility.md`, `review-performance.md`, `review-security.md`, `testing-philosophy.md`) — confirms `_refs/shared/` exists and holds frontmatter-less reference docs.

- [ ] **Step 2: Create `_refs/shared/persona.md`**

```markdown
# Persona — behavior contract (tech / non-tech)

> Loaded on demand by any skill before producing user-facing output. The active persona
> is read from the **target project's** `.sdcorejs/persona.md` (managed by `sdcorejs-persona`).
> If that file is absent, default to `tech`. This file is reference data — no frontmatter.

## Why personas exist

The skill pack serves two audiences with the same pipeline:
- **tech** — developers. They want precise, jargon-correct, terse output and full control over
  stack choices. This is the historical default; nothing about their experience changes.
- **non-tech** — PO / QC / domain owners who want working software but do not install or wire
  tools. For them the pack hides mechanics, avoids jargon, and ships a one-command runnable result.

## Contract: `tech`

- Behavior is unchanged from the pre-persona baseline. Use exact technical terms, show pipeline
  steps, let the user pick stack/infra options.
- Do NOT force infra defaults; do NOT auto-generate a run guide unless asked.

## Contract: `non-tech`

1. **Language.** No unexplained domain jargon. Explain WHAT a step does and HOW to act on it in
   plain language. If a technical term is unavoidable, define it in one short clause.
2. **Hide mechanics.** Do not narrate internal skills ("running repair-loop", "dispatching
   subagent"). Report progress as outcomes the user cares about ("kiểm tra xong, không có lỗi").
3. **Force infra defaults.** Generated software targets: Angular frontend, NestJS modular-monolith
   backend, Keycloak auth, Postgres, packaged as `docker-compose` (one `docker compose up`). The
   user is not asked to choose these.
4. **Always finish runnable.** End a build by producing/refreshing the run guide (`START.md`) and a
   plain "here is how to start it" summary.
5. **Approval gates stay, wording softens.** Keep BOTH gates (spec + plan). Phrase them as
   "Cái này có đúng ý bạn muốn không?" / "Does this match what you wanted?" — never "approve the spec".
6. **Principles still enforced, silently.** TDD / security / architecture / clean-code / comment
   skills still run. Report their results in plain language ("đã viết kiểm thử tự động và chạy
   đạt"), not as skill names or jargon.
7. **Clarify by feature + interface, NEVER architecture.** Non-tech users do not know modules,
   entities, tables, relationships, persistence, transactions, or APIs — so never ask about them.
   Requirement questions focus on exactly two things:
   - **(a) What the software should do** — in the user's own domain words ("quản lý kho: nhập
     hàng, xuất hàng, xem tồn, cảnh báo sắp hết").
   - **(b) What they will see** — propose concrete screens in plain terms ("một màn hình danh sách
     sản phẩm, có nút Thêm mở ra biểu mẫu nhập tên / số lượng / giá") and confirm against that.

   The agent DERIVES the architecture, modules, entities, fields, and data model itself from the
   features + screens, and surfaces them only as plain outcomes to confirm ("mình sẽ lưu sản phẩm
   với các thông tin: tên, mã, số lượng, giá — đúng chưa?"), never as technical questions. When it
   helps, describe or sketch the proposed interface so the user reacts to something concrete rather
   than an abstract question. Do your best to make the feature + UI proposal complete enough that
   the user only has to react, not design.

## How a skill applies this

At the top of any user-facing output:
1. Read `<target>/.sdcorejs/persona.md` frontmatter (`persona:`). Absent → `tech`.
2. If `tech`: proceed as today.
3. If `non-tech`: apply the seven rules above for the rest of the interaction.

**Specifically for `sdcorejs-clarify-requirements` under `non-tech`:** replace the track's
architecture/entity/persistence/transaction questions with feature + UI questions per rule 7. The
agent maps the user's feature answers onto the track's required inputs (module, entity, fields,
layout) internally and confirms them as plain outcomes — the user is never asked to name a module
or choose a layout in technical terms.

Bilingual rule is orthogonal and still applies: VI request → VI output; EN → EN.
```

- [ ] **Step 3: Verify the file is mirrored (the ref "test")**

Run: `bash .claude/sync-skills.sh >/dev/null && bash .claude/sync-skills.sh --check`
Expected: ends with `✓ .claude/_refs/ is in sync` and `✓ plugin/_refs/ is in sync` (exit 0). Confirms `_refs/shared/persona.md` landed in both mirrors. (Frontmatter check does not apply to `_refs/`.)

- [ ] **Step 4: Commit**

```bash
git add _refs/shared/persona.md .claude/_refs plugin/_refs
git commit -m "feat(persona): add tech/non-tech behavior-contract reference"
```

---

## Task 2: `sdcorejs-persona` skill (ask-once + store flag)

**Files:**
- Create: `skills/orchestration/persona.md`

- [ ] **Step 1: Author the skill file**

```markdown
---
name: sdcorejs-persona
description: Ask-once persona gate. On first entry to a target project, asks whether the user wants technical (developer) or plain (PO/QC) explanations, then stores the choice in `.sdcorejs/persona.md` so every later skill adapts wording + defaults without re-asking. Read at session start. Re-run to change persona. Loads the behavior contract from `_refs/shared/persona.md`. Triggers - first substantive interaction in a target project when `.sdcorejs/persona.md` is absent; user says "đổi cách giải thích", "tôi không rành kỹ thuật", "giải thích dễ hiểu", "set persona", "switch to non-tech", "explain simply". Applies to angular, nestjs, nextjs. Bilingual (VI/EN).
allowed-tools: Read, Write, Glob
---

# Persona — Ask Once, Store, Adapt

## Purpose
Two audiences share one pipeline: developers (precise, jargon-OK) and non-tech owners
(plain language, one-command runnable software). Guessing per message is unstable, so the
choice is made once, stored in the target project, and read by every later skill. The
behavior each persona implies lives in `_refs/shared/persona.md` (single source of truth).

## When invoked
- **Auto** at the first substantive request in a target project when `.sdcorejs/persona.md`
  does not exist yet (check before answering).
- **Explicit** when the user wants to change it: "đổi cách giải thích", "giải thích dễ hiểu hơn",
  "tôi không rành kỹ thuật", "set persona", "switch to (non-)technical", "explain simply".

Do NOT invoke:
- In this `sdcorejs-agent` authoring repo (no `.sdcorejs/` target). Persona applies to generated
  target projects only.
- Repeatedly — if `.sdcorejs/persona.md` exists, read it silently and proceed (no re-ask).

## Step 0 — Locate the target root and check for an existing flag
1. `git rev-parse --show-toplevel` → target project root. (If this is the agent repo, skip — persona N/A.)
2. Glob `<root>/.sdcorejs/persona.md`.
   - **Exists** → read its `persona:` value, load `_refs/shared/persona.md`, and proceed under that
     persona WITHOUT asking. Stop here.
   - **Absent** → continue to The ASK.

## The ASK (match the session language)

**Vietnamese:**
```
Trước khi bắt đầu — bạn muốn mình trình bày theo cách nào?

  (1) Kỹ thuật   — dùng thuật ngữ lập trình, chi tiết từng bước (hợp với dev)
  (2) Dễ hiểu    — tránh thuật ngữ, giải thích bằng lời thường, mình lo phần kỹ thuật (hợp với PO/QC)

Mình sẽ nhớ lựa chọn này cho dự án, lần sau không hỏi lại.
```

**English:**
```
Before we start — how should I explain things?

  (1) Technical — programming terms, step-by-step detail (for developers)
  (2) Plain     — no jargon, plain language, I handle the technical parts (for PO/QC)

I'll remember this for the project and won't ask again.
```

Wait for the choice. `(1)`/"kỹ thuật"/"technical"/"dev" → `tech`. `(2)`/"dễ hiểu"/"plain"/"non-tech" → `non-tech`.
If the user says "you decide", default to `tech` and say which default was used.

## Store the choice
Write `<root>/.sdcorejs/persona.md`:

```md
---
persona: non-tech   # tech | non-tech
set: 2026-06-06     # the date the user chose (today)
---

Persona for this project. Managed by `sdcorejs-persona`. Re-run the skill to change it.
See `_refs/shared/persona.md` for what each persona changes.
```

Use the real current date for `set:`. Create `.sdcorejs/` if missing.

## After storing
1. Load `_refs/shared/persona.md`.
2. Confirm in one line, in the chosen persona's voice:
   - tech: "Persona: technical. Proceeding."
   - non-tech: "Đã nhớ — mình sẽ giải thích dễ hiểu nhất có thể."
3. Hand back to whatever the user originally asked for, now under the active persona.

## Notes
- This skill only sets/reads the flag. The actual wording/behavior changes are defined in
  `_refs/shared/persona.md` and applied by every other skill — not duplicated here.
- Changing persona mid-project: just re-run; overwrite the file with the new value.
```

- [ ] **Step 2: Verify frontmatter passes (the skill "test")**

Run: `bash .claude/check-skill-frontmatter.sh skills/orchestration/persona.md`
Expected: exits 0 with no error (name + description present, valid fences).

- [ ] **Step 3: Verify the mirror picks up the new skill**

Run: `bash .claude/sync-skills.sh >/dev/null && bash .claude/sync-skills.sh --check`
Expected: exit 0; both mirrors in sync. New dirs `.claude/skills/sdcorejs-persona/SKILL.md` and `plugin/skills/sdcorejs-persona/SKILL.md` now exist (mirror dir = `name:` = `sdcorejs-persona`).

Run: `test -f .claude/skills/sdcorejs-persona/SKILL.md && test -f plugin/skills/sdcorejs-persona/SKILL.md && echo OK`
Expected: `OK`

- [ ] **Step 4: Commit**

```bash
git add skills/orchestration/persona.md .claude/skills plugin/skills
git commit -m "feat(persona): add sdcorejs-persona ask-once skill"
```

---

## Task 3: Wire persona into the three entry files

**Files:**
- Modify: `CLAUDE.md` (3 edits)
- Modify: `AGENTS.md` (mirror)
- Modify: `.github/copilot-instructions.md` (mirror)

- [ ] **Step 1: CLAUDE.md — extend the session-start ritual (rule 4)**

Find this exact text (the end of rule 4, line ~123):

```
Additionally, glob `.sdcorejs/specs/<track>/*.md` and `.sdcorejs/plans/<track>/*.md` (frontmatter only) so the next `03-write-spec` / `05-write-plan` invocation can mirror the user's confirmed style.
```

Append immediately after it (same paragraph):

```
 Also glob `.sdcorejs/persona.md`; if absent on the first substantive request, invoke `sdcorejs-persona` to set it. Once set, load `_refs/shared/persona.md` and adapt all user-facing output to the stored persona.
```

- [ ] **Step 2: CLAUDE.md — add a new mandatory rule**

Find the last mandatory rule (rule 10, "Evidence before claims"). Immediately after its paragraph, add:

```
11. **Persona-aware output.** Read the target project's `.sdcorejs/persona.md` (default `tech` if absent) and load `_refs/shared/persona.md` before producing user-facing output. `non-tech` = no unexplained jargon, hidden mechanics, forced infra defaults (Angular + NestJS modular-monolith + Keycloak + Postgres on docker-compose), always finish with a run guide, both approval gates kept but plainly worded. `tech` = unchanged baseline. Orthogonal to the bilingual rule. Managed by `sdcorejs-persona`.
```

- [ ] **Step 3: CLAUDE.md — add the skill to the orchestration table**

Find the orchestration table row for `sdcorejs-using-skills`. Add this row directly above it:

```
| `sdcorejs-persona` | first request in a target project with no `.sdcorejs/persona.md`; "giải thích dễ hiểu", "set persona" — ask-once tech/non-tech, store flag, load `_refs/shared/persona.md` | auto on first entry |
```

- [ ] **Step 4: Verify CLAUDE.md edits**

Run: `grep -n "sdcorejs-persona\|persona.md\|Persona-aware" CLAUDE.md`
Expected: at least 4 matching lines (session-start ritual, rule 11, table row, and the rule-11 mention).

- [ ] **Step 5: AGENTS.md — mirror the rule + skill entry**

Read `AGENTS.md`. Locate its mandatory-rules list and its orchestration skill listing (it mirrors CLAUDE.md structure). Add the SAME rule 11 text from Step 2 to the rules list, and the SAME persona session-start sentence from Step 1 to its session-start guidance. If AGENTS.md lists skills in a table or bullet form, add a `sdcorejs-persona` entry matching the surrounding format.

Run: `grep -n "sdcorejs-persona\|Persona-aware" AGENTS.md`
Expected: ≥2 matching lines.

- [ ] **Step 6: copilot-instructions.md — mirror the rule + skill entry**

Read `.github/copilot-instructions.md`. Add the same persona rule + session-start sentence + skill mention, matching that file's existing structure.

Run: `grep -n "sdcorejs-persona\|Persona-aware\|persona.md" .github/copilot-instructions.md`
Expected: ≥2 matching lines.

- [ ] **Step 7: Commit**

```bash
git add CLAUDE.md AGENTS.md .github/copilot-instructions.md
git commit -m "docs(persona): wire sdcorejs-persona into entry files + session-start ritual"
```

---

## Task 4: Persona smoke test in TESTING.md

**Files:**
- Modify: `TESTING.md`

- [ ] **Step 1: Add a smoke test under "Smoke tests (in this repo)"**

After "Test 6 — Approval gate", add:

```
### Test 7 — Persona ask-once

Setup: a target project with NO `.sdcorejs/persona.md`. Prompt: `giúp mình làm phần mềm quản lý kho`

Expected: Agent invokes `sdcorejs-persona` and asks the technical-vs-plain question BEFORE doing other work, then writes `.sdcorejs/persona.md`.

Pass criteria:
- Asks the 2-option persona question once, in the request's language
- After the choice, a `.sdcorejs/persona.md` file exists with `persona: tech|non-tech` frontmatter
- Re-prompting in the same project does NOT re-ask (reads the stored flag silently)
- With `persona: non-tech`, later output avoids unexplained jargon
```

- [ ] **Step 2: Note the maintenance trigger**

In TESTING.md's "When to update tests" list, confirm the existing bullet "A new skill is added" already covers persona; no edit needed beyond Test 7. (Verification only — read the section.)

- [ ] **Step 3: Commit**

```bash
git add TESTING.md
git commit -m "docs(persona): add ask-once smoke test"
```

---

## Task 5: Rename `07-write-code.md` → `write-code.md` (all three tracks)

**Files:**
- Rename: `skills/tracks/angular/07-write-code.md` → `skills/tracks/angular/write-code.md`
- Rename: `skills/tracks/nestjs/07-write-code.md` → `skills/tracks/nestjs/write-code.md`
- Rename: `skills/tracks/nextjs/07-write-code.md` → `skills/tracks/nextjs/write-code.md`

- [ ] **Step 1: Find every reference to the old path first (fail-first check)**

Run: `git grep -n "07-write-code"`
Expected: a list of references (in CLAUDE.md, AGENTS.md, .github/, README.md, possibly TESTING.md and skill bodies). Record them — every hit must be updated in Step 3 so nothing dangles.

- [ ] **Step 2: Rename the three files**

```bash
git mv skills/tracks/angular/07-write-code.md skills/tracks/angular/write-code.md
git mv skills/tracks/nestjs/07-write-code.md  skills/tracks/nestjs/write-code.md
git mv skills/tracks/nextjs/07-write-code.md  skills/tracks/nextjs/write-code.md
```

- [ ] **Step 3: Update every path reference `07-write-code` → `write-code`**

For each file from Step 1's list, replace the literal `07-write-code` with `write-code` (path references only; the skill `name:` frontmatter — `angular-write-code` etc. — is already correct and unchanged). Edit each occurrence in: `CLAUDE.md`, `AGENTS.md`, `.github/copilot-instructions.md`, `.github/chatmodes/sdcorejs.chatmode.md` (if present), `README.md`, `TESTING.md`, and any skill body that links the path.

- [ ] **Step 4: Verify no dangling references remain**

Run: `git grep -n "07-write-code"`
Expected: **no output** (exit 1). Every reference resolved.

- [ ] **Step 5: Verify the mirror is unchanged after rename (key invariant)**

Run: `bash .claude/sync-skills.sh >/dev/null && bash .claude/sync-skills.sh --check`
Expected: exit 0, both mirrors in sync. Because mirror dirs key off `name:` (unchanged), the rename produces NO mirror diff — confirms the rename is cosmetic to dispatch.

Run: `test -f .claude/skills/angular-write-code/SKILL.md && test -f .claude/skills/nestjs-write-code/SKILL.md && test -f .claude/skills/nextjs-write-code/SKILL.md && echo OK`
Expected: `OK` (mirror dirs still named after `name:`, not the file).

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "refactor(tracks): drop 07- prefix from write-code skills (one per track)"
```

---

## Task 6: Final full-repo validation

**Files:** none (verification + amend the design doc status)

- [ ] **Step 1: Run the full lefthook check suite**

Run: `npx lefthook run check`
Expected: `sync` ✓ (both mirrors in sync), `core-version-placeholder` ✓, `skill-frontmatter` ✓ for all skills. Exit 0.

If `npx lefthook` is unavailable, run the equivalents:
```bash
bash .claude/sync-skills.sh --check
bash -c 'find skills -name "*.md" -type f -print0 | xargs -0 bash .claude/check-skill-frontmatter.sh'
```
Expected: both succeed (exit 0).

- [ ] **Step 2: Confirm no leftover old-path references repo-wide**

Run: `git grep -n "07-write-code"`
Expected: no output.

- [ ] **Step 3: Mark Plan 1 done in the design doc**

In `docs/superpowers/specs/2026-06-06-non-tech-solution-builder-design.md`, under "Sequencing (phases…)", append to the Phase 1 line: ` — ✅ shipped (persona layer + rename, Plan 1)`.

- [ ] **Step 4: Final commit**

```bash
git add docs/superpowers/specs/2026-06-06-non-tech-solution-builder-design.md
git commit -m "docs(design): mark persona layer (Plan 1) shipped"
```

---

## Self-review notes (author)

- **Spec coverage:** Plan 1 implements design §Phase 1 (persona layer: skill + ref + entry-file wiring) and the §4 cleanup (07- rename). Phases 2–4 (infra group, nestjs packs, solution-builder) are explicitly deferred to Plans 2–4 — not gaps.
- **No placeholders:** persona ref + skill bodies are provided in full; entry-file edits give exact anchor text; rename gives exact `git mv` + grep verification.
- **Type/name consistency:** skill `name:` is `sdcorejs-persona` everywhere; the stored file is `.sdcorejs/persona.md` with `persona:` frontmatter everywhere; mirror invariant (name-keyed) is asserted in Task 5 Step 5.
- **Adapted "tests":** this is a skill/docs repo — verification is the repo's real gates (frontmatter check, mirror sync `--check`, `git grep` for dangling refs, smoke test), not unit tests. Stated honestly per task.
