# SDCoreJS Agent Handoff - 2026-04-22

## Session Focus

Release preparation for Angular Portal starter + sample app parity.

Main goals completed:
- Add new `customer` sample entity module.
- Fix detail-page data visibility regression (`code`, `name`) for department/customer.
- Sync starter template with runtime portal implementation.
- Update skill rules to prevent recurrence.

## Completed Work

### 1. New sample entity: customer
Implemented full CRUD module in portal sample architecture.

Added files (runtime + starter parity):
- `src/libs/sample/modules/customer/customer.routes.ts`
- `src/libs/sample/modules/customer/index.ts`
- `src/libs/sample/modules/customer/services/customer.model.ts`
- `src/libs/sample/modules/customer/services/customer.mock-data.ts` (100 seed rows)
- `src/libs/sample/modules/customer/services/customer.service.ts`
- `src/libs/sample/modules/customer/pages/list/list.component.ts`
- `src/libs/sample/modules/customer/pages/detail/detail.component.ts`

Routing/menu wiring:
- Added `customer` child route in `src/libs/sample/routes.ts`
- Added `Customers` menu in `src/app/components/main/main.component.ts`

### 2. Detail-page regression fix (department + customer)
Issue observed: opening stale detail IDs could show empty placeholders and hide key values.

Fixes applied:
- Switched detail rendering to `viewed` form-control pattern so `code`/`name` remain visible and consistent.
- Added stale-ID fallback in detail loaders:
  - Catch `detail(id)` not-found errors
  - Show warning notification
  - Navigate user back to list route (`../../`) with replace-tab state

Updated files:
- `src/libs/sample/modules/department/pages/detail/detail.component.ts`
- `src/libs/sample/modules/customer/pages/detail/detail.component.ts`

### 3. Mock data resiliency fix
Issue observed: if localStorage payload existed as empty array/corrupted JSON, table/list stayed empty and detail IDs became invalid.

Fix applied:
- Enhanced `MockCrudStore.#ensureSeeded()` to reseed when persisted value is:
  - missing
  - empty array (`[]`)
  - corrupted JSON

Updated file:
- `src/libs/sample/services/base/mock-crud.store.ts`

### 4. Starter template synchronization
Synchronized the following from runtime portal app into starter template:
- `src/libs/sample/modules/customer/**`
- `src/libs/sample/modules/department/pages/detail/detail.component.ts`
- `src/libs/sample/services/base/mock-crud.store.ts`
- `src/libs/sample/routes.ts`
- `src/app/components/main/main.component.ts`

Note:
- Intentionally did not overwrite starter `README.md` in this pass to preserve latest user-side edits.

### 5. Skill library updates
Updated skills to encode new guardrails from this incident:

Updated files:
- `skills/angular-portal/angular-entity-crud-skill.md`
  - Require mock reseed behavior for missing/empty/corrupted localStorage.
  - Require stale-ID recovery in detail flow.
  - Require guaranteed code/name visibility in detail mode.
- `skills/angular-portal/entity-crud-generation-skill.md`
  - Add generation rule for mock reseed behavior.
  - Add detail fallback pattern for `Entity not found`.
  - Add validation checklist items for both.

## Verification

### Build
- `portal-agency`: `npm run build-dev` passed after fixes.

### Runtime
- `/sample/customer` route available and menu visible.
- Customer list displays seeded rows.
- Customer detail opens from list row.
- Department/customer stale-detail handling now recovers to list with warning.

## Deploy Checklist (Release Candidate)

1. Build checks
- Run `npm --prefix <portal-app> run build-dev`
- Optional: run production build target if your pipeline requires it.

2. Smoke checks
- Home page loads.
- 5 sample menus visible: Employees, Products, Departments, Customers, Sale Events.
- Department/customer detail from list row shows code/name.
- Product side-drawer flow still works.
- Sale-event anchor flow still works.

3. Data checks
- Confirm localStorage auto-reseed when `sample-mock-*` values are empty/corrupted.
- Confirm list pages recover seeded data without manual reset.

4. Template checks
- Ensure starter includes customer module + route/menu wiring.
- Ensure starter `mock-crud.store.ts` includes reseed behavior.

## Recommended Next Actions

1. Run one full starter-generation test from `core/templates/angular-portal-starter`.
2. Validate `npm run start` + smoke script in freshly generated repo.
3. Tag release after confirming CI build parity between runtime app and starter template.
