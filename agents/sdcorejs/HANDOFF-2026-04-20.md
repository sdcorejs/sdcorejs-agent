# SDCoreJS Agent Handoff - 2026-04-20

## 1) Session Goal

Finalize the first release-ready baseline for SDCoreJS Angular Portal agent:
- align skill library behavior with real generation patterns
- enforce cross-model consistency (Claude Code, Gemini, Codex)
- ensure prompt packs and smoke tests reflect latest rules
- capture working context so work can continue smoothly on another machine

## 2) What Was Updated

### A. Core behavior and governance updates

1. Added mandatory generation pipeline and global guardrails in portal skills.
2. Added cross-model compatibility contract to reduce model drift.
3. Added direct GitHub-link usage guidance for Claude Code.
4. Added detail-layout intelligence:
   - UnifiedCompact
   - UnifiedSplit
   - AdaptiveSplitDetail
5. Added sd-anchor-v2 guidance for long multi-section page forms.

### B. New/updated prompts

1. Cross-model starter prompt:
- .github/prompts/sdcorejs-angular-portal.prompt.md

2. New Claude-specific prompt (GitHub link mode):
- .github/prompts/sdcorejs-angular-portal-claude-github.prompt.md

3. Smoke test prompt packs extended with portal-init tsconfig hygiene case:
- .github/prompts/sdcorejs-angular-portal-smoke-tests.prompt.md
- .github/prompts/sdcorejs-angular-portal-smoke-tests.vi.prompt.md

### C. Skill updates relevant to release quality

1. Portal init skill now includes tsconfig hygiene rule:
- remove compilerOptions.baseUrl when not required by import patterns
- keep paths aliases explicit and valid without baseUrl coupling

File:
- skills/angular-portal/angular-portal-project-init-skill.md

2. Request intake and entity CRUD were already updated earlier to support:
- module-first clarification
- mock-first CRUD (localStorage default when no API contract)
- layout variant clarification
- anchor-enabled long-form UX guidance

## 3) Verified State

### Build/validation snapshots

1. portal-ops build status:
- npm run build -> PASS (warnings only from Sass deprecations/CommonJS dependencies)

2. tsconfig baseUrl experiment:
- Removed compilerOptions.baseUrl from portal-ops tsconfig
- Rebuilt successfully
- Conclusion: baseUrl is optional and should be omitted unless imports require it

## 4) Release Checklist (Ready-to-run)

1. In sdcorejs-agent repo:
- run docs sanity pass
- confirm no contradictory rule remains between README/INDEX/skills/prompts

2. Run smoke tests (EN + VI):
- use prompt packs in .github/prompts/
- evaluate with:
  - agents/sdcorejs/SMOKE-TEST.md
  - agents/sdcorejs/SMOKE-TEST.vi.md

3. Confirm cross-model envelope consistency:
- Resolved Context
- Planned Skill Chain
- Files To Create/Update
- Post-Gen Double Check

4. Confirm portal-init tsconfig hygiene:
- no unnecessary compilerOptions.baseUrl
- if baseUrl exists, model must explain why

## 5) Known Risks / Open Items

1. Existing docs still contain legacy BaseService examples in some long reference sections (historical examples).
- Impact: low-medium (can confuse newcomers).
- Suggested next step: run one cleanup pass to mark those examples as API-contract mode only.

2. Old locked folder in another workspace path may still require manual process cleanup:
- portal-template/.generated may remain locked by external process.
- Not blocking sdcorejs-agent release.

## 6) Suggested Next Actions at Home

1. Do a final consistency grep:
- find references that imply BaseService is always default
- keep wording as: mock-first default, BaseService when API contract is explicit

2. Execute smoke test prompts in 2 model families:
- Claude Code via GitHub-link prompt
- one of Gemini/Codex via cross-model starter prompt

3. Tag first release after checklist passes.

## 7) Handy Commands

```powershell
# Open repo
Set-Location "c:\Users\nghiatt15_onemount\Documents\sdcorejs\sdcorejs-agent"

# Quick search for potential contradictions
rg "Service extending BaseService|extends BaseService|localStorage mock CRUD|baseUrl" skills .github agents

# Optional: show changed files
git status --short
```

## 8) Quick Resume Context

If resuming in a new machine/session, start with:
1. Read this file first.
2. Read agents/sdcorejs/README.md (cross-model + Claude link mode).
3. Use .github/prompts/sdcorejs-angular-portal-claude-github.prompt.md for Claude tests.
4. Run the smoke packs and mark pass/fail in SMOKE-TEST files.
